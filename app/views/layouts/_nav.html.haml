%nav.top-bar{data: {topbar: true, options: 'is_hover: false'}}
  %ul.title-area
    %li.name
      %h1
        = link_to 'Home', root_path
  %section.top-bar-section
    -# Left Nav Section
    %ul.left
      - if is_admin?
        %li.divider
        %li
          = link_to 'Admin', admin_path
      %li.divider
    -# Middle
    %ul.middle

    -# Right Nav Section
    %ul.right
      %li.divider
      %li
        = link_to 'Bugs', bugs_path
      %li.divider
      %li
        = link_to 'Drugs', bug_classes_path
      %li.divider
      %li
        = link_to 'Symptoms', bug_classes_path
      %li.divider
      %li
        = link_to 'Syndromes', bug_classes_path
      %li.divider      
      - if @current_user
        %li.has-dropdown
          = link_to '#' do
            = @current_user.username
          %ul.dropdown
            %li
              = link_to account_path do
                %i.fi-torso
                Account
            %li
              = link_to login_path, method: :delete do
                %i.fi-power
                Log out
      - else
        %li
          = link_to new_user_path do
            %i.fi-pencil
            Register
        %li.divider
        %li
          = link_to login_path do
            %i.fi-power
            Log in
      %li.divider
