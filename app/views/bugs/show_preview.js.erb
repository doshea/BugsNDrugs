if($('#bug_<%= @id %> .preview').length < 1){
  var new_preview = $("<%=j render partial: 'bugs/partials/preview' %>").hide();
  $('#bug_<%= @id %> .preview-container').append(new_preview);
  new_preview.show();
}