= render partial: 'layouts/errors', locals: {error_object: new_drug}
= form_for new_drug, url: admin_drugs_path, method: :post, remote: true do |f|
  = render partial: 'admin/drugs/partials/form_contents', locals: {f: f}