var new_row = $("<%=j render partial: 'admin/bugs/partials/tr', locals: {bug: @bug} %>").hide();
new_row.prependTo($('table')).fadeIn(300);
var new_form = $("<%=j render partial: 'admin/bugs/partials/form', locals: {new_bug: @new_bug} %>");
$('#new_bug').after(new_form).remove();