if($('#drug_<%= @id %> .preview').length < 1){
  var new_preview = $("<%=j render partial: 'partials/preview' %>").hide();
  $('#drug_<%= @id %> .preview-container').append(new_preview);
  new_preview.show();
}