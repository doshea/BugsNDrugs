if($('#drug_<%= @id %> .preview .nib').length < 1){
  var new_preview = $("<%=j render partial: 'drugs/partials/preview' %>").hide();
  $('#drug_<%= @id %> .preview').append(new_preview);
  new_preview.show();
}